config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 500
  http:
    maxSockets: 10
    timeout: 30
scenarios:
  - name: "Get Meta"
    flow:
      - get:
          url: "/reviews/meta"
          qs:
            product_id: 324244

